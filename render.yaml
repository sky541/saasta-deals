services:
  - type: web
    name: saasta-deals
    env: python
    buildCommand: "pip install flask werkzeug"
    startCommand: "python3 -c \"from web_app import app; app.run(host='0.0.0.0', port=$PORT)\""
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
