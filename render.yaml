services:
  - type: web
    name: grabcoupon
    env: python
    buildCommand: "pip install -r deals_bot/requirements.txt"
    startCommand: "cd deals_bot && python -m web_app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
