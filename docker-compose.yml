version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    
  scheduler:
    build: .
    command: python scheduler.py --interval 12
    volumes:
      - ./data:/app/data
    restart: unless-stopped
